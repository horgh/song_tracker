Requirements:
 - PHP 5.3+
 - MySQL (recent version?)

Setup:
 1. Create a database and user.
  mysql> create database songs;
	mysql> create user 'songs'@'localhost' identified by 'songs';
  mysql> grant all on songs.* to 'songs'@'localhost';

 2. Import schema.sql to this database.
  ~$ mysql -u songs -p songs < code/song_tracker/website/schema.sql 

 3. Place website/ directory in a webserver accesible location.
 4. Visit http://your.website/music/register.php to register.
 5. Begin updating to api.php!
